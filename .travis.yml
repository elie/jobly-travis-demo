language: node_js
node_js:
  - '14'
services:
  - postgresql
before_install:
- cd backend
before_script:
  - psql -c 'create database jobly_test;' -U postgres
  - psql jobly_test < jobly.sql
install:
- npm install
- npm install --save-dev jest
script:
- npm test